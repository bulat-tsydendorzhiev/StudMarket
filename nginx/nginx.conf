server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript image/svg+xml;
    gzip_min_length 1024;

    # API Gateway proxy. Strips the /api prefix: /api/auth/me -> /auth/me
    location /api/ {
        proxy_pass http://api-gateway:8000/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # SPA: unknown paths fall back to index.html (client-side routing)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Static assets get long-lived caching; index.html stays short-lived.
    location /assets/ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}

# For VPS deployment add the HTTPS server block below, reference your
# certificates and redirect HTTP -> HTTPS:
#
# server {
#     listen 443 ssl http2;
#     server_name example.com;
#
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#
#     # ... same root, /api/ and /assets/ locations as above ...
# }
#
# server {
#     listen 80;
#     server_name example.com;
#     return 301 https://$host$request_uri;
# }